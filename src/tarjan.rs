use std::collections::VecDeque;

struct references {
    n: usize,
    graph: Vec<Vec<usize>>,
    solved: bool,
    scc_count: usize,
    id: usize,
    visited: Vec<bool>,
    ids: Vec<usize>,
    low: Vec<usize>,
    sccs: Vec<usize>,
    stack: VecDeque<usize>,
    unvisited: f64 = -1; 
}
fn TarjanSccSolverAdjacencyList(Vec<Vec<usize>>) {
    if (references::graph.is_empty()){
        println!
    }
    n = graph.size();
    this.graph = graph;
  }


